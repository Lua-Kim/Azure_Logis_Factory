import ModuleCard from "../common/ModuleCard.jsx";

const CenterList = ({ children }) => (
  <ModuleCard title="CenterList" description="Center list with paging">
    {children}
  </ModuleCard>
);

export default CenterList;
