import ModuleCard from "../common/ModuleCard.jsx";

const SectionTab = ({ children }) => (
  <ModuleCard title="SectionTab" description="Section configuration">
    {children}
  </ModuleCard>
);

export default SectionTab;
