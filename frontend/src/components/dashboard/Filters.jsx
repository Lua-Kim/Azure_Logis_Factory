import ModuleCard from "../common/ModuleCard.jsx";

const Filters = () => (
  <ModuleCard title="Filters" description="Shared dashboard filters">
    <p className="muted">Filter controls placeholder.</p>
  </ModuleCard>
);

export default Filters;
