Bicep를 사용하면 Azure 리소스를 쉽고 빠르게 만들 수 있다.
사용은 Azure Portal 에서 CLI 통해서 명령어 입력하면 된다.
어떻게 만들었냐에따라 다르지만 기본 리소스 이름 물어보면 입력하면 된다.

Fabric EventStream
실시간 스트림을 Fabric에 연결할 수 있는데,
거기서 연결한때 사용자 이름/암호가 사실은 연결정보를 쓰는 것이다. 
번역 이상함.

ASA에서 병목 지연 판정